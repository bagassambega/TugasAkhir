\subsection{Implementasi Proxy Re-encryption Server}

Proxy re-encryption server atau PRE diimplementasikan menggunakan bahasa pemrograman Rust dengan Axum sebagai framework HTTP. PRE berperan sebagai komponen penghubung antara client pasien, client tenaga kesehatan, IPFS, Redis, dan smart contract IOTA. PRE tidak menyimpan plaintext PGHD. Komponen ini menerima payload PGHD terenkripsi, melakukan verifikasi, menyimpan ciphertext ke IPFS, mencatat metadata ke smart contract, dan menyediakan material re-encryption ketika tenaga kesehatan yang berwenang membuka data PGHD.

PRE menggunakan Redis untuk menyimpan material akses sementara, seperti token akses dan material re-encryption. Material tersebut memiliki waktu kedaluwarsa sehingga akses yang diberikan pasien tidak dapat digunakan di luar periode yang ditentukan. PRE juga menggunakan IOTA Gas Station ketika perlu membuat transaksi IOTA bersponsor.

\subsubsection{Endpoint PRE}

Endpoint utama yang digunakan pada implementasi PGHD dapat dilihat pada Tabel~\ref{tab:implementasi-endpoint-pre-pghd}.

\begin{styledxltabular}{|>{\raggedright\arraybackslash}p{0.16\textwidth}|>{\raggedright\arraybackslash}p{0.28\textwidth}|>{\raggedright\arraybackslash}X|}
    \hiderowcolors
    \caption{Endpoint utama PRE untuk DecMed-PGHD}\label{tab:implementasi-endpoint-pre-pghd} \\
    \hline
    \rowcolor{headerBg}
    \textbf{Method} & \textbf{Endpoint} & \textbf{Fungsi} \\
    \hline
    \showrowcolors
    \endfirsthead
    \hline
    \rowcolor{headerBg}
    \textbf{Method} & \textbf{Endpoint} & \textbf{Fungsi} \\
    \hline
    \endhead
    \hline
    \endfoot
    \hline
    \endlastfoot
    \texttt{GET} & \texttt{/api/v1/health} & Memeriksa status service PRE. \\
    \hline
    \texttt{POST} & \texttt{/api/v1/nonce} & Membuat nonce untuk autentikasi pasien. Nonce disimpan pada Redis dengan TTL 3 menit. \\
    \hline
    \texttt{POST} & \texttt{/api/v1/keys} & Menyimpan material re-encryption dan membuat token akses baca, update, atau baca PGHD. \\
    \hline
    \texttt{POST} & \texttt{/api/v1/keys/revoke} & Menghapus material re-encryption dari Redis ketika akses dicabut. \\
    \hline
    \texttt{POST} & \texttt{/api/v1/pghd/patient} & Menyimpan public key PGHD pasien pada PRE sebagai cache tambahan. \\
    \hline
    \texttt{POST} & \texttt{/api/v1/pghd/submit} & Menerima batch PGHD terenkripsi dari client Android, memverifikasi signature, menyimpan ciphertext ke IPFS, dan mencatat metadata ke IOTA. \\
    \hline
    \texttt{GET} & \texttt{/api/v1/pghd} & Mengambil daftar metadata PGHD pasien untuk tenaga kesehatan yang memiliki akses \texttt{READ\_PGHD}. \\
    \hline
    \texttt{GET} & \texttt{/api/v1/pghd/\{index\}} & Mengambil detail PGHD, melakukan verifikasi hash, membuat \textit{cfrag}, dan mengembalikan payload terenkripsi beserta material dekripsi kepada client tenaga kesehatan. \\
    \hline
    \texttt{POST} & \texttt{/api/v1/pghd/invalidate} & Mengubah status metadata PGHD menjadi invalid pada smart contract. \\
    \hline
    \texttt{GET} & \texttt{/api/v1/gen/proxy-address} & Membuat dan mendaftarkan alamat proxy/capability yang digunakan PRE untuk memanggil fungsi tertentu pada smart contract. \\
    \hline
\end{styledxltabular}

\subsubsection{Alur Submit PGHD}

Endpoint \texttt{POST /api/v1/pghd/submit} menerima batch PGHD dari client Android. PRE terlebih dahulu memvalidasi alamat IOTA pasien dan melakukan decode ciphertext PGHD dari format Base64. Setelah itu, PRE menghitung hash SHA-256 dari ciphertext dan membandingkannya dengan \texttt{h\_cipher} yang dikirim client. Jika hash tidak sesuai, request ditolak.

Jika hash sesuai, PRE mengambil public key PGHD pasien dari smart contract atau cache Redis. Public key tersebut digunakan untuk memverifikasi outer signature PGHD. Verifikasi ini memastikan bahwa ciphertext PGHD benar-benar dibuat oleh pasien pemilik key PGHD. Setelah signature valid, PRE menyimpan ciphertext ke IPFS dan memperoleh CID. CID, hash, signature, capsule, dan metadata batch kemudian dikemas sebagai metadata PGHD dan dicatat ke smart contract melalui fungsi \texttt{submit\_pghd}.

\subsubsection{Alur Baca PGHD}

Endpoint baca PGHD hanya dapat dipanggil dengan token akses yang memiliki purpose \texttt{ReadPghd}. Saat tenaga kesehatan meminta daftar PGHD, PRE memeriksa token akses dan memastikan material re-encryption untuk pasangan pasien dan tenaga kesehatan masih tersedia pada Redis. Setelah itu, PRE memanggil smart contract untuk mengambil daftar metadata PGHD yang masih berstatus valid.

Saat tenaga kesehatan membuka satu entri PGHD, PRE mengambil ciphertext dari IPFS berdasarkan CID yang tercatat di IOTA. PRE kemudian menghitung hash ciphertext dan membandingkannya dengan hash on-chain. Jika hash tidak cocok, PRE menginvalidasi entri PGHD tersebut dan mengembalikan error kepada client tenaga kesehatan. Jika hash cocok, PRE membuat \textit{cfrag} dari \textit{kfrag} yang tersimpan pada Redis dan mengembalikannya kepada client tenaga kesehatan. Client tenaga kesehatan kemudian melakukan dekripsi akhir dan verifikasi inner signature.

\subsubsection{Material Akses Sementara}

Material akses sementara disimpan pada Redis dengan key yang mengandung tujuan akses, alamat tenaga kesehatan, dan alamat pasien. Untuk akses PGHD, key tersebut menggunakan purpose \texttt{read\_pghd}. Material yang disimpan meliputi \textit{kfrag}, public key pasien, public key data, public key penanda tangan, dan ciphertext seed data yang diperlukan untuk membuka payload. Jika akses dicabut atau TTL habis, material tersebut tidak lagi tersedia sehingga tenaga kesehatan tidak dapat melakukan dekripsi PGHD meskipun masih memiliki ciphertext.
